# Profile Photo

Please add your profile photo here:

1. Save your photo as `profile-photo.jpg` in the `public` folder
2. Recommended size: 400x400 pixels (square)
3. Supported formats: JPG, PNG, WebP
4. The image will be automatically cropped to a circle

If the image is not found, the component will fallback to showing "MP" initials.

